<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.hyj.lib"
    android:versionCode="1"
    android:versionName="1.0.1">

    <uses-sdk
        android:minSdkVersion="14"
        android:targetSdkVersion="18" />

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />

    <application
        android:name=".LibApplication"
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainLibActivity"
            android:label="@string/app_name"
            android:launchMode="singleTask">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 星型菜单 -->
        <activity
            android:name=".startmenu.StartMenu"
            android:launchMode="singleTask" />
        <activity
            android:name=".startmenu.StartMenu2"
            android:launchMode="singleTask" />

        <!-- 倒计时 -->
        <activity
            android:name=".ui.TimerCountActivity"
            android:launchMode="singleTask" />

        <!-- 主界面实现方案合集 -->
        <activity
            android:name=".SecondaryActivity"
            android:launchMode="singleTask" />

        <!-- Fragment实现Tab功能 -->
        <activity
            android:name=".mainview.tabfragment.FragmentTabActivity"
            android:launchMode="singleTask" />

        <!-- 仿微信界面 -->
        <activity
            android:name=".mainview.wechat.WeChatActivivty"
            android:launchMode="singleTask" />

        <!-- qq5.0侧滑菜单 -->
        <activity
            android:name=".mainview.qq5_0.SlidingActivity"
            android:launchMode="singleTask" />

        <!-- ViewPager切换动画 -->
        <activity
            android:name=".viewpager.ViewPagerActivity"
            android:launchMode="singleTask" />

        <!-- ViewPager切换动画,自定义向下兼容 -->
        <activity
            android:name=".viewpager.ViewPagerCustormerActivity"
            android:launchMode="singleTask" />

        <!-- 自定义Title测试 -->
        <activity
            android:name=".title_bar.TitleBarActivity"
            android:launchMode="singleTask" />

        <!-- 图片美化处理 -->
        <activity
            android:name=".image_mt.ImageMain"
            android:launchMode="singleTask" />
        <activity
            android:name=".image_mt.PrimaryColorActivity"
            android:launchMode="singleTask" />
        <activity
            android:name=".image_mt.ColorMatrixActivity"
            android:launchMode="singleTask" />
        <activity
            android:name=".image_mt.PixelsActivity"
            android:launchMode="singleTask" />

        <!-- 树形节点设计 -->
        <activity
            android:name=".tree.TreeActivity"
            android:launchMode="singleTask" />

        <!-- 万能适配器 -->
        <activity
            android:name=".adapter.AdapterAcitivty"
            android:launchMode="singleTask" />

        <!-- 轮播广告图片 -->
        <activity
            android:name=".imagecycle.ImageCycleViewActivity"
            android:launchMode="singleTask" />

        <!-- 文件断点续传下载 -->
        <activity
            android:name=".downservice.DownServiceActivity"
            android:launchMode="singleTask" />

        <service
            android:name=".http.download.DownService"
            android:launchMode="singleTask" />

        <!-- 流式布局 -->
        <activity
            android:name=".flowlayout.FlowLayoutActivity"
            android:launchMode="singleTask" />

        <!-- 下拉刷新组件 -->
        <activity
            android:name=".listviewrefresh.ListViewRfreshActivity"
            android:launchMode="singleTask" />

        <!-- 九宫格 -->
        <activity
            android:name=".lock.LockActivity"
            android:launchMode="singleTask" />
        <activity
            android:name=".lock.LockQuestionActivity"
            android:launchMode="singleTask" />
        <activity
            android:name=".lock.lockpattern.LockPatternActivity"
            android:launchMode="singleTask" />
        <activity
            android:name=".lock.lockpattern2.LockTestActivity"
            android:launchMode="singleTask" />
        <activity
            android:name=".lock.lockpattern3.Lock3Activity"
            android:launchMode="singleTask" />

        <!-- 仿微信图片上传 -->
        <activity
            android:name=".wechat_imageUp.ImageLoaderActivity"
            android:launchMode="singleTask" />

        <!-- 仿微信聊天 -->
        <activity
            android:name=".wechat_talk.WeChatTalkActivity"
            android:launchMode="singleTask" />

        <!-- 个性图片预览与多点触控 -->
        <activity
            android:name=".image_preview.ImagePreviewActivity"
            android:launchMode="singleTask" />

        <!-- 刮刮卡 -->
        <activity
            android:name=".scratch.ScratchCardActivity"
            android:launchMode="singleTask" />

        <!-- 带索引条的ListView -->
        <activity
            android:name=".listviewindex.ListViewIndexActivity"
            android:launchMode="singleTask" />

        <!-- DialogUtils工具类测试 -->
        <activity
            android:name=".ui.DialogActivity"
            android:launchMode="singleTask" />

        <!-- 抽奖转盘 -->
        <activity
            android:name=".luckydial.LuckyDialActivity"
            android:launchMode="singleTask" />

        <!-- RecyclerView测试 -->
        <activity
            android:name=".recyclerview.RecyclerActivity"
            android:launchMode="singleTask" />
        <activity
            android:name=".recyclerview.StaggeredGridLayoutActivity"
            android:launchMode="singleTask" />

        <!-- AndroidAnnotaion框架测试 -->
        <activity
            android:name=".annotaionsframe.AnnotationsActivity_"
            android:launchMode="singleTask" />

        <!-- 自定义相机测试 -->
        <activity
            android:name=".camera.CameraMainActivity"
            android:launchMode="singleTask" />
        <activity
            android:name=".camera.CamaraCustomActivity"
            android:launchMode="singleTask" />
        <activity
            android:name=".camera.CameraResultActivity"
            android:launchMode="singleTask" />

        <!-- 图灵机器人测试 -->
        <activity
            android:name=".tuling.TulingActivity"
            android:launchMode="singleTask" />

        <!-- 拼图游戏 -->
        <activity
            android:name=".jigsaw.JigsawActivity"
            android:launchMode="singleTask" />

        <!-- Widget桌面小控件 -->
        <activity
            android:name=".widget.WidgetActivity"
            android:launchMode="singleTask" />

        <service android:name=".widget.TimerService" />

        <receiver android:name=".widget.WidgetProvider">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>

            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widgetconfig" />
        </receiver>

        <!-- 心愿分享 -->
        <activity
            android:name=".wish.WishActivity"
            android:launchMode="singleTask"
            android:theme="@android:style/Theme.NoTitleBar.Fullscreen" />

        <!-- 注解使用 -->
        <activity
            android:name=".annotation.AnnotationActivity"
            android:launchMode="singleTask" />

        <!-- 自定义ViewPagerIndicator指示器 -->
        <activity
            android:name=".indicator.ViewPagerIndicatorActivity"
            android:launchMode="singleTask" />

        <!-- 五子棋游戏 -->
        <activity
            android:name=".gobang.GobangActivity"
            android:launchMode="singleTask" />

        <!-- 高清加载巨图方案 -->
        <activity
            android:name=".largeImage.LargeImageViewActivity"
            android:launchMode="singleTask" />
        <activity
            android:name=".largeImage.LargeImageViewSimpleBakActivity"
            android:launchMode="singleTask" />

        <!-- 弹出Popup实现方案合集 -->
        <activity
            android:name=".popup.PopupActivity"
            android:launchMode="singleTask" />

        <!-- GridView的Item设置成正方形 -->
        <activity
            android:name=".ui.SquareActivity"
            android:configChanges="orientation|keyboardHidden"
            android:launchMode="singleTask" />

        <!-- 二维码扫描 -->
        <activity
            android:name=".zxing.ZxingActivity"
            android:launchMode="singleTask" />

        <!-- 圆形图片 -->
        <activity
            android:name=".circleimageview.CircleImageActivity"
            android:launchMode="singleTask" />

        <!-- Activity与service交互测试 -->
        <activity
            android:name=".ActivityService.MessengerActivit"
            android:launchMode="singleTask" />
        <service android:name=".ActivityService.MessengerService" />
    </application>

</manifest>